# AUDIO FIX v2
기존 저장소의 파일을 이 폴더 내용으로 덮어쓰세요.

특히 `index.html`, `sw.js`, `manifest.json`, `audio/test.wav`가 모두 올라가야 합니다.

배포 후:
1. https://pina-web.github.io/musicplayer/?v=2 로 접속
2. `소리 테스트`를 누름
3. 테스트음이 들리면 `음악 시작`
4. 홈 화면에 이미 설치한 이전 PWA가 있다면 삭제 후 다시 홈 화면에 추가
